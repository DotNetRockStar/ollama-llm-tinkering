apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: istio-gateway
spec:
  selector:
    istio: gateway
  servers:
  - port:
      number: 41123
      name: http41123
      protocol: HTTP
    hosts:
    - "*"
  - port:
      number: 80
      name: http80
      protocol: HTTP
    hosts:
    - "*"
  - port:
      number: 1111
      name: http1111
      protocol: HTTP
    hosts:
    - "*"